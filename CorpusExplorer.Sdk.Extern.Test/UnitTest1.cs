#region

using Microsoft.VisualStudio.TestTools.UnitTesting;

#endregion

namespace CorpusExplorer.Sdk.Extern.Test
{
  [TestClass]
  public sealed class UnitTest1
  {
    [TestMethod]
    public void KidkoImporterTest()
    {
      /*
      var importer = new KidkoScraper();
      var docs = importer.Import("testdata/KiDKo_E_Tabelle.xlsx");
      Assert.IsNotNull(docs);
      Assert.AreNotEqual(0, docs.Count());

      var parser = new TreeTaggerClassicParser();
      parser.ScraperResults = docs;
      parser.Execute();
      */
    }
  }
}